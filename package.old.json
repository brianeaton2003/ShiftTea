{
  "name": "shifttea",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix shifttea",
    "dev": "npm run dev --prefix shifttea",
    "build": "npm run build --prefix shifttea",
    "deploy:dev": "cd firebase && npx firebase deploy --project dev",
    "deploy:prod": "cd firebase && npx firebase deploy --project prod"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "firebase-tools": "^15.13.0",
    "wait-on": "^8.0.3"
  },
  "dependencies": {
    "firebase": "^12.11.0"
  }
}
